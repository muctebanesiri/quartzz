---
title: بهینه‌سازی مدل‌های زبانی
draft: false
tags: 
date: 2024-10-20 11:21
---
بهینه‌سازی مدل‌های موجود زمانی لازم می‌شود که اطلاعات موردنظر ما در دیتاست‌های [[Training|آموزش]] مدل‌ها موجود نباشند. این عدم وجود اغلب به خاطر **تخصصی** یا **تازه** بودن اطلاعات است. برای حل این مشکل و فهماندن اطلاعات به مدل‌ها، دو رویکرد اصلی وجود دارد: [[Retrieval-Augmented Generation (RAG)]] و [[Fine-tuning]][^1].

استفاده از رویکرد RAG زمانی به‌صرفه است که دقت اطلاعات خروجی برای ما مهم باشد. اگر دقت مهم نیست و صرفاً کلیتی مدنظر ماست -مثلاً اینکه مدل به سبک یک نویسندهٔ خاص صحبت کند- بهتر است از فاین‌تیونینگ استفاده کنیم.

## تکنیک RAG

به زبان ساده، RAG اسناد مرتبط را از ما می‌گیرد و تلاش می‌کند که محتویات هر یک را بفهمد. بعد از فهمیدن، با هر بار درخواست کاربر، سیستم RAG اسناد مرتبط با درخواست را پیدا می‌کند و آن‌ها را در کنار درخواست اولیهٔ کاربر (مثل تقلب) به مدل زبانی می‌دهد.

| ![[RAG-Arch.png]]               |
| ------------------------------- |
| <center>معماری کلی RAG</center> |

پیاده‌سازی سیستم RAG با ابزارهای موجود به نسبت ساده است. در ادامه پیاده‌سازی یک سیستم ساده RAG با کمک `Ollama` و `Open WebUI` و سپس پیاده‌سازی آن را با زبان `python` می‌بینیم.

نصب `Ollama` دشواری خاصی ندارد. با مراجعه به [صفحهٔ دانلود](https://ollama.com/download) آن نسخهٔ موردنظر را انتخاب، دانلود و نصب کنید. برای نصب `Open WebUI` هم بعد از نصب `python` و `pip`، کافیست دستور `pip install open-webui` را در ترمینال اجرا کنید تا نصب آن کامل شود.

بعد از نصب نرم‌افزارهای موجود، نوبت به دریافت مدل می‌رسد. فهرست مدل‌های موجود `Ollama` در [این صفحه](https://ollama.com/library) قابل مشاهده است. ما در این مدل برای سادگی کار، با مدل `Llama 3.1` کار می‌کنیم. برای دریافت این مدل، دستور `ollama pull llama3.1` را در ترمینال اجرا می‌کنیم.

![[Pasted image 20241020125943.png]]

حالا که مدل و رابط کاربری هر دو آماده هستند، نوبت به اجرای آن‌ها می‌رسد. با اجرای `ollama run llama3.1` مدل در ترمینال اجرا می‌شود. برای راه‌اندازی رابط کاربری هم ابتدا دستور `open-webui serve` را اجرا کنید و بعد از طریق آدرس `http://localhost:8080` در مرورگر آن را باز کنید.


| ![[webui-1.jpg]]            |
| --------------------------- |
| <center>Open WebUI</center> |

ابزار `Open WebUI` قابلیت دریافت اسناد و اجرای RAG را دارد. برای اضافه کردن فایل‌ها، به منوی `Knowledge` بروید و یک پوشهٔ جدید ایجاد کنید. سپس آدرس فایل‌های خود را به آن بدهید تا فایل‌ها را یکی یکی [[Embedding|Embed]] کند. زمانی که این فرآیند به پایان رسید، به صفحهٔ اصلی `Open WebUI` برگردید و این‌بار دستور خود را با @ وارد کنید تا اطلاعات جدید در پاسخ مدل فراخوانی شوند.


| ![[webui-2.jpg]]                             |
| -------------------------------------------- |
| <center>بخش مربوط به اسناد در WebUI</center> |


|                       | روش RAG | بهینه‌سازی با LoRA | بهینه‌سازی پایه |
| --------------------- | ------- | ------------------ | --------------- |
| پیاده‌سازی            | ساده    | دشوار              | دشوار           |
| هزینه و زمان          | کم      | متوسط              | زیاد            |
| اضافه کردن دادهٔ جدید | آسان    | دشوار              | دشوار           |
| امکان توهم مدل        | پایین   | بالا               | بالا            |

[^1]: البته که راه سومی هم وجود دارد و آن ساخت یک مدل از پایه است. اما این راه به دلیل حجم بالای پردازشی و اطلاعاتی، به هیچ وجه در مقیاسی جز مقیاس ملی صرفه ندارد.